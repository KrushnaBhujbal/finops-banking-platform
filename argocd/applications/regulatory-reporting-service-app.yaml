apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: regulatory-reporting-service
  namespace: argocd
spec:
  project: reporting
  source:
    repoURL: https://github.com/KrushnaBhujbal/finops-banking-platform.git
    targetRevision: main
    path: helm/charts/generic-microservice
    helm:
      valueFiles:
        - "../../values/dev/regulatory-reporting-service.yaml"
  destination:
    server: https://kubernetes.default.svc
    namespace: reporting
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
