apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: credit-scoring-service
  namespace: argocd
spec:
  project: risk
  source:
    repoURL: https://github.com/KrushnaBhujbal/finops-banking-platform.git
    targetRevision: main
    path: helm/charts/generic-microservice
    helm:
      valueFiles:
        - "../../values/dev/credit-scoring-service.yaml"
  destination:
    server: https://kubernetes.default.svc
    namespace: risk
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
